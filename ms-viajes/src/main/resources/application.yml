server:
  port: 8083

spring:
  application:
    name: ms-viajes
  
  # IMPORTANTE: Cambiar entre MySQL y H2 usando profiles
  # Para MySQL (default): no hacer nada
  # Para H2 (desarrollo sin MySQL): agregar en IDE/CLI: -Dspring.profiles.active=h2
  
  datasource:
    # MySQL (PRODUCCION Y DESARROLLO CON LARAGON)
    url: jdbc:mysql://localhost:3306/viajesdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password:
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  
  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    use-root-path: true
microservicios:
  pasajeros:
    url: http://localhost:8081
  conductores:
    url: http://localhost:8082
  tarifas:
    url: http://localhost:8085
---
# PERFIL H2: para desarrollo sin MySQL instalado
spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:file:./data/ms-viajesdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
